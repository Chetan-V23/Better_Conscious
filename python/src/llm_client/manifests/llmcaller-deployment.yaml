apiVersion: apps/v1
kind: Deployment
metadata:
  name: llmcaller
  labels:
    app: llmcaller
spec:
  replicas: 2
  selector:
    matchLabels:
      app: llmcaller
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 8
  template:
    metadata:
      labels:
        app: llmcaller
    spec:
      containers:
        - name: llmcaller
          image: r0dent/llmcaller
          ports:
            - containerPort: 5679
          envFrom:
            - configMapRef:
                name: llmcaller-configmap
            - secretRef:
                name: llmcaller-secret
