apiVersion: v1
kind: Service
metadata:
  name: gateway

spec:
  selector:
    app: gateway
  type: ClusterIP
  ports:
    - protocol: TCP
      name: http
      port: 8080
      targetPort: 8080
    - name: debug
      port: 5678
      targetPort: 5678